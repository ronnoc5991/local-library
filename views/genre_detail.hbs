<h1> {{ genre.name }} </h1>

<h4> Books </h4>

<dl>
    {{#each genre_books}}
        <dt>
            <a href={{this.url}}> {{ this.title }} </a>
        </dt>
        <dd>
            {{ this.summary }}
        </dd>
    {{/each}}
</dl>